syntax = "proto3";

option go_package = "components/proto";

enum StatusCode {
    OK = 0;
    INTERNAL_ERROR = 1;
    INVALID_TYPE = 2;
    ALREADY_EXISTS = 3;
    NOT_FOUND = 4;
}

message Status {
    StatusCode code = 1;
    string message = 2;
}
