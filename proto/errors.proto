syntax = "proto3";

option go_package = "components/proto";

enum StatusCode {
    OK = 0;
    INVALID_TYPE = 1;
    ALREADY_EXISTS = 2;
    DOES_NOT_EXIST = 3;
}

message Status {
    StatusCode code = 1;
    string message = 2;
}
